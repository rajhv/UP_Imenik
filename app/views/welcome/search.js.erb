var html = "<%= escape_javascript(render "employees/table", locals: { employees: @employees }) %>"; // load employee table partial -> http://api.rubyonrails.org/classes/ActionView/Helpers/JavaScriptHelper.html
var employeeTableContainer = $("#employee-table-container"); // find the element that holds our employee table

employeeTableContainer.html(html); // replace the existing table with the partial html rendered from partial above
