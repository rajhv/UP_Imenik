$('.delete_department').bind('ajax:success', function() {
    var selectedRowToDelete = $(this).closest('tr')
    selectedRowToDelete.fadeOut();
    console.log("vrstica: " + selectedRowToDelete)
});
